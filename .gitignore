# Local image-generation LLM credentials
.env.imagegen.local

# Cached upstream data for scripts/build-sentences.mjs
.cache/

# Build output & deps
node_modules/
dist/

# AVIF/WebP derivatives are generated with `npm run images`, but they are
# committed because clean GitHub Actions checkouts must include deployable assets.

# Generated: copied from src/content/sentences.json by scripts/sync-data.mjs
public/data/

# Playwright MCP scratch output
.playwright-mcp/

# Single-file preview build
dist-standalone/
